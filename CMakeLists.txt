cmake_minimum_required(VERSION 3.16)

project(AnimalXCOM VERSION 0.1 LANGUAGES CXX)

set(CMAKE_CXX_STANDARD_REQUIRED ON)
set(CMAKE_AUTORCC ON)
find_package(Qt6 6.5 REQUIRED COMPONENTS Quick Concurrent Svg)

qt_standard_project_setup(REQUIRES 6.5)

qt_add_executable(appAnimalXCOM
    main.cpp
)

qt_add_qml_module(appAnimalXCOM
    URI AnimalXCOM
    VERSION 1.0
    QML_FILES
        Main.qml
        SOURCES
        SOURCES roadgenerator.h roadgenerator.cpp
        SOURCES roadsdrawer.h roadsdrawer.cpp
        SOURCES macros.h
        SOURCES terraingenerator.h terraingenerator.cpp
        RESOURCES images.qrc
)

# Qt for iOS sets MACOSX_BUNDLE_GUI_IDENTIFIER automatically since Qt 6.1.
# If you are developing for iOS or macOS you should consider setting an
# explicit, fixed bundle identifier manually though.
set_target_properties(appAnimalXCOM PROPERTIES
#    MACOSX_BUNDLE_GUI_IDENTIFIER com.example.appAnimalXCOM
    MACOSX_BUNDLE_BUNDLE_VERSION ${PROJECT_VERSION}
    MACOSX_BUNDLE_SHORT_VERSION_STRING ${PROJECT_VERSION_MAJOR}.${PROJECT_VERSION_MINOR}
    MACOSX_BUNDLE TRUE
    WIN32_EXECUTABLE TRUE
)

target_link_libraries(appAnimalXCOM
    PRIVATE Qt6::Quick
    Qt6::Concurrent
    Qt6::Svg
)

include(GNUInstallDirs)
install(TARGETS appAnimalXCOM
    BUNDLE DESTINATION .
    LIBRARY DESTINATION ${CMAKE_INSTALL_LIBDIR}
    RUNTIME DESTINATION ${CMAKE_INSTALL_BINDIR}
)
